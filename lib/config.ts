export const uiConfig = {
  // Global UI feature flags
  // Set to false to disable hover/click animations on retro components
  animatedEffects: false,
  // Set to false to disable game sounds globally
  enableSounds: true,
};
